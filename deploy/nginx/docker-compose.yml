services:
  nginx:
    image: nginx:1.27-alpine
    container_name: crackoa-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./crackoa.conf:/etc/nginx/conf.d/default.conf:ro
